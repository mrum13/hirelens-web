name: hirelens_admin
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  cupertino_icons: ^1.0.8
  d_method: ^0.2.6
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.2.1
  go_router: ^16.2.0
  google_fonts: ^6.3.0
  intl: ^0.20.2
  supabase_flutter: ^2.9.1

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  # ✅ Daftarkan file .env agar bisa diakses di Flutter Web dan Mobile
  assets:
    - .env
